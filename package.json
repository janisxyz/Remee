{
  "name": "remee-programmer-archive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline preservation tooling for the Remee optical programmer",
  "scripts": {
    "check": "node --check scripts/archive.mjs && node --check scripts/sanitize.mjs && node --check scripts/validate-offline.mjs && node --check scripts/validate-live.mjs && node --check scripts/offline-shim.js",
    "archive": "node scripts/archive.mjs",
    "sanitize": "node scripts/sanitize.mjs",
    "validate": "node scripts/validate-offline.mjs",
    "validate:live": "node scripts/validate-live.mjs"
  },
  "dependencies": {
    "playwright": "1.55.0"
  }
}
